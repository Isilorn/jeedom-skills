# Session 2026-04-27 — Skeleton skill J1

## Objectif de la session

Poser le skeleton de la skill : SKILL.md validé, connexion SSH+MySQL stabilisée avec user read-only dédié, `db_query.py` opérationnel sur box réelle, tests unitaires passants.

## Décisions prises pendant la session

- **`sql-cookbook.md` reporté en J2** : le cookbook est construit itérativement avec les scripts qui l'utilisent (`resolve_cmd_refs.py`, etc.) — plus efficace qu'à vide en J1.
- **`sudo mysql` requis pour créer le user RO** : le user `jeedom` Jeedom n'a pas le privilège `CREATE USER`. La création a été déléguée au PO (une seule commande `sudo mysql`), puis tout le reste automatisé.
- **`ssh.mysql_json` parse le TSV via python3 côté serveur** : approche retenue pour éviter de dépendre d'une lib MySQL locale sur la machine Claude Code. Compromis : python3 doit être disponible sur la box (cas standard sur Debian/Ubuntu Jeedom).
- **Venv `.venv/` pour les tests** : environnement Python isolé, exclu du git via `.gitignore`.
- **PLANNING gotcha #4 corrigé dans SKILL.md** : jMQTT topic dans `configuration.topic` (cmd) et `configuration.mqttIncTopic` (broker), pas dans `logicalId` comme indiqué à tort dans le PLANNING.

## Découvertes / surprises

- **`lastLaunch` et `state` sont absents de la DB `scenario`** : champs runtime retournés uniquement par `scenario::byId` (API). Tous les workflows qui en ont besoin (WF2) doivent passer par l'API, pas MySQL.
- **`collectDate`/`valueDate` n'existent pas** : ni en DB ni via l'API `cmd::byId`. Référence supprimée du WF3 dans SKILL.md.
- **Tables utiles non documentées dans `audit_db.md`** : `config` (version Jeedom, clé API), `message` (messages système), `update` (versions plugins) — ajoutées à la hiérarchie §5 de SKILL.md.
- **Schéma DB officiel** confirmé via `database.json` branche `V4-stable` du repo `jeedom/core` — `audit_db.md` empirique était fiable.
- **`debian.cnf` inaccessible sans sudo** : pas de raccourci pour le bootstrap MySQL — délégation PO inévitable.
- **Clé API Jeedom chiffrée en DB** : déchiffrement via `php -r "define('ROOT',...); require_once('.../core.inc.php'); echo config::byKey('api');"` — intégré dans `setup.py`.

## Travail réalisé

- Cross-check `audit_db.md` vs schéma officiel `V4-stable` (github.com/jeedom/core) vs box réelle
- `jeedom-audit/SKILL.md` (250 lignes, 11 sections)
- `jeedom-audit/scripts/_common/__init__.py`
- `jeedom-audit/scripts/_common/credentials.py`
- `jeedom-audit/scripts/_common/ssh.py`
- `jeedom-audit/scripts/_common/tags.py`
- `jeedom-audit/scripts/_common/sensitive_fields.py`
- `jeedom-audit/scripts/_common/version_check.py`
- `jeedom-audit/scripts/db_query.py` (testé sur box réelle)
- `jeedom-audit/scripts/setup.py` (interactif, détection automatique credentials)
- `jeedom-audit/references/connection.md`
- `tests/unit/test_db_query.py` (18 tests, 18/18 passants)
- Credentials configurés : `jeedom_audit_ro` MySQL RO, `~/.my.cnf` box, `credentials.json` local

## Reste à faire (J1)

Rien — J1 fermé.

## Pour la prochaine session (J2)

**Commencer par :** `references/sql-cookbook.md` — c'est le premier livrable J2 et le socle de tous les workflows DB.

**Contexte technique à avoir en tête :**
- `db_query.py` est opérationnel — utiliser `run(query, params, creds)` directement
- `trigger` est auto-backtické dans `db_query.py` — pas besoin de l'échapper manuellement dans les requêtes passées à `run()`
- `lastLaunch`/`state` → API uniquement (`scenario::byId`)
- Topic jMQTT → `configuration.topic` (cmd), `configuration.mqttIncTopic` (broker)
- `ssh.mysql_json` retourne du JSON parsé — les rows sont des dicts avec toutes les colonnes

**Ordre recommandé J2 :**
1. `references/sql-cookbook.md`
2. `scripts/resolve_cmd_refs.py` + tests
3. `scripts/scenario_tree_walker.py` + tests
4. `references/scenario-grammar.md`
5. WF5 end-to-end sur box réelle
6. `references/audit-templates.md` + `references/health-checks.md`
7. WF1 end-to-end sur box réelle

## Pour le PO

- **À J2** : valider les résultats de WF1 (audit général) et WF5 (explication scénario) exécutés par Claude Code sur la box réelle.
